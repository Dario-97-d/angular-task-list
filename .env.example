# Local development (used by `netlify dev`)
API_URL=http://localhost:3000/tasks

# API_KEY is not needed locally — json-server has no authentication.
# In production (Netlify dashboard), both API_URL and API_KEY are set
# to point at restdb.io and its required key.